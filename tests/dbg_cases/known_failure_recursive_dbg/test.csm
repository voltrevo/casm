// Known failure: dbg should work consistently in recursive calls
i32 countdown(i32 n) {
    dbg(n);
    if (n == 0) {
        return 0;
    }
    return countdown(n - 1);
}

i32 main() {
    return countdown(2);
}
